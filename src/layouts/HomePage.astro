---
import type { MDXLayoutProps } from "astro";
import ProsePage from "./ProsePage.astro";

type Props = MDXLayoutProps<{
  title: string;
}>;

const { frontmatter } = Astro.props;
---

<ProsePage title={frontmatter.title}>
  <slot />
</ProsePage>
